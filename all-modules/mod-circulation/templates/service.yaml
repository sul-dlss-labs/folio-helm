apiVersion: v1
kind: Service
metadata:
  name: {{ include "mod-circulation-old.fullname" . }}
  labels:
    {{- include "mod-circulation-old.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.containerPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "mod-circulation-old.selectorLabels" . | nindent 4 }}
